{
    // TODO create a new file without `-recommend` suffix and copy the content.
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build compiler",
            "type": "shell",
            "command": "cmake --build build -j 4",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}